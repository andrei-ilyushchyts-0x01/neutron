schema: neutron.research-scenarios/v1
scenarios:
  - id: bluetooth-default
    root_package: probe
    follow_services: true
    follow_hal: true
    trace: { driver_packs: [binder], syscalls: [29, 198, 206, 207, 208, 209], resolve_paths: true }
    stimulus: { action: bluetooth, timeout: 15s }
    duration: 6s
    settle_ms: 500
    calibration: { scan_ms: "3000", identifiers_retained: "false", enable_radio: "false" }
